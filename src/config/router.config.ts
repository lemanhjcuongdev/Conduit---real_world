const routerConfig = {
  home: "/",
  login: "/login",
  register: "/register",
  settings: "/settings",
  editor: "/editor",
  editorExistArticle: "/editor/:slug",
  profile: "/:profile",
  articleDetail: "/articles/:slug",
};
export default routerConfig;
